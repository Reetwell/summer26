# Build Your Body — status snapshot (2026-07-10)

Full-directory pass over `summer26/` (web app, backend, native app, marketing, mockups).
Grounded in `git status`/`git diff` and the project docs. Newest snapshot wins — for the
living plan see the brain note `Projects/Build-Your-Body.md`.

## 🟡 In progress (uncommitted work on disk)

- **Native app — major redesign pass, uncommitted.** 11 Swift files (+673/−560):
  `TrainingView` (major), `ProgressDashboardView`, `LeagueView`/`RankSystem`, `TodayView`,
  `RecipeStore`, `ShoppingStore`, `MealsView`, `TrainingStore`. Commit before it's at risk.
- **`backend/wearables.js`** — reworked (+447), uncommitted.
- **`backend/worker.js`** — modified (+23), uncommitted (mounts new endpoints + CORS fix).
- **`backend/account.js`** — new, untracked (the `/account/delete` deletion endpoint).
- **`styles.css`** — modified (~48), uncommitted.
- **`marketing/`** — entire folder untracked (campaign assets + compliance drafts).

## 🔴 Not done (open items)

**Hard launch gates (block public release)**
- Privacy policy **published** — draft exists, needs legal review + hosting at `/privacy.html`
- Data-deletion — code done, needs **one real end-to-end test** + Path B email backstop
- **Resend sender domain** verified (no new user gets a code until this)
- **Remove dev-skip bypass** (`index.html:104` / `app.js:3208`)
- **Lock app name + trademark check** — schedule long pole (see name-candidates doc)
- **App Store Connect + Apple Developer enrolment** (start now — dead wait)
- **Worker deploy** with `wearables.js` + `account.js` + secrets; resolve the deploy-status
  discrepancy (is `/account/delete` actually live?)

**Backend / data**
- Run the **4 Supabase SQL files**; decide **paid tier**
- **Verify sync actually writes** (`app_data=0` — possible silent bug)
- Loosen strict password rules
- Move **WorkoutX key** behind the Worker before any paid tier

**Native**
- Parity **QA** (HealthKit really reads; swap / done-tracking / reminders end-to-end)
- Migrate brand tokens to the **#00694c** ramp

**Gated features waiting on keys**
- Un-gate **Recipes** (needs `ANTHROPIC_API_KEY`)
- Flip `WEARABLES_ENABLED = true` (needs Oura/Whoop/Fitbit dev apps + secrets)

**Planned-next (from note):** Apple Health integration, metric/imperial units, daily
reminder notifications, data export.

## 💡 Ideas to add (net-new, not yet tracked)

1. **Automated Supabase backup/export** — insurance against free-tier pause data loss + `app_data=0`.
2. **Client error monitoring** (lightweight) — no visibility into in-the-wild errors today.
3. **Name candidates + trademark pre-check now** — the long pole (→ `marketing/name-candidates-trademark-check.md`).
4. **TestFlight beta with real 14–21 users** before public — de-risks Apple review + gets feedback.
5. **Under-16 consent path** — 13+ checkbox is a start; UK GDPR wants parental consent under 16.
6. **AADC-safe analytics** (no profiling) — currently flying blind on activation/retention.
7. **Pre-deploy smoke-test checklist** — no test suite; catch the "silent sync broke" class.
8. **One secrets/env checklist doc** for the Worker deploy — secrets scattered across files.
9. **Accessibility + Dynamic Type pass** — App Store + audience table stakes.
